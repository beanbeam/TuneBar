import Cocoa

enum Constants {
    static let menuBarTrackTitleFont =
        NSFont.systemFont(ofSize: 12, weight: NSFont.Weight.bold)

    static let menuBarArtistFont =
        NSFont.systemFont(ofSize: 12, weight: NSFont.Weight.thin)
}
